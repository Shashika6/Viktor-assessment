export const styles = {
  loadingContainer: {
    display: 'flex',
    justifyContent: 'center',
    py: 8,
  },

  errorContainer: {
    textAlign: 'center',
    py: 4,
  },

  emptyContainer: {
    textAlign: 'center',
    py: 4,
  },

  grid: {
    display: 'grid',
    gridTemplateColumns: 'repeat(auto-fill, minmax(300px, 1fr))',
    gap: 3,
  },
};

